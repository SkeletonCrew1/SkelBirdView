---
webservers:
  hosts:
    web-server-1:
      ansible_host: 10.3.11.249

    web-server-2:
      ansible_host: 10.3.11.115

databases:
  hosts:
    database-1:
      ansible_host: 10.3.11.219

load_balancers:
  hosts:
    load-balancer-1:
      ansible_host: 10.3.1.137

  #vars:
  #  StrictHostKeyChecking: no
#    ansible_user: "{{ lookup('env', 'jenkins_user') }}"
#    ansible_ssh_pass: "{{ lookup('env', 'jenkins_pass') }}"
