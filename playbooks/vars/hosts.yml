---
webservers:
  hosts:
    web-server-1:
      ansible_host: 10.3.11.59

    web-server-2:
      ansible_host: 10.3.11.20

databases:
  hosts:
    database-1:
      ansible_host: 10.3.11.53

load_balancers:
  hosts:
    load-balancer-1:
      ansible_host: 10.3.11.20

  #vars:
  #  StrictHostKeyChecking: no
#    ansible_user: "{{ lookup('env', 'jenkins_user') }}"
#    ansible_ssh_pass: "{{ lookup('env', 'jenkins_pass') }}"
