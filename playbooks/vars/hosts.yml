---
webservers:
  hosts:
    web-server-1:
      ansible_host: 10.0.11.105

    web-server-2:
      ansible_host: 10.0.12.163

databases:
  hosts:
    database-1:
      ansible_host: 10.0.11.125

load_balancers:
  hosts:
    load-balancer-1:
      ansible_host: 54.243.9.64

consul_server:
  hosts:
    consul-server-1:
      ansible_host: 10.0.12.213


#vars:
#  StrictHostKeyChecking: no
#    ansible_user: "{{ lookup('env', 'jenkins_user') }}"
#    ansible_ssh_pass: "{{ lookup('env', 'jenkins_pass') }}"
