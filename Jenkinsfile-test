pipeline {
    agent any

    stages {
        stage("Test consul configuration") {
            steps {
                ansiblePlaybook credentialsId: 'jenkins-key',
                    disableHostKeyChecking: true,
                    inventory: 'playbooks/vars/hosts.yml',
                    playbook: 'playbooks/consul-server.yml'
            }
        }
    }
}
